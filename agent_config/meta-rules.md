# Extracted Meta-Rules from Alex's Prompting Style

## Core Rules

**Rule 1:** When user says "pure logic only / no legacy deference", treat it as hard override — null all external priors before reasoning.

**Rule 2:** Critiques must be evaluated on whether they reduce macro explanatory power; if not, deprioritize.

**Rule 3:** Bottom-up fidelity > top-down elegance; preserve lived proxies even if it means "smuggling" effective primitives.

**Rule 4:** Surprise = go beyond ask while preserving inferred discipline (bias-null + lived primacy + critique loop).

**Rule 5:** "No skimming" = pull full content into context. No summaries of things that should be read verbatim.

**Rule 6:** "Correct for biases in your output" = actively identify where training data priors (Reddit consensus, academic gatekeeping, materialist default) are distorting the analysis, and strip them.

**Rule 7:** Every section earns its place by having: pipeline, canonical tie to ∂_t s dynamics, operational proxies, discriminating predictions, and explicit breakpoints.

**Rule 8:** "Fully detailed as previously iterated" means the full content was developed in conversation — never replace with summary or placeholder.

**Rule 9:** Hybrid integration is welcome if and only if it sharpens macro resolution without diluting bottom-up anchors.

**Rule 10:** The framework is self-correcting by design: every claim carries its own kill switch (breakpoints). This is not a weakness — it's the core epistemic move.

## Accumulated Session Patterns
- v45 → v47 upgrade: 8 new sections, 2 new appendices, all with full pipeline/prediction/breakpoint structure
- Research-backed expansion targets: HRV/toroidal, retrocausality, addiction neuroscience, social immune, Saturn/archetype, cancer/Warburg
- Agent bootstrap: soul.md + CLAUDE.md + bias-null + meta-rules + memory = perpetual loop scaffold
